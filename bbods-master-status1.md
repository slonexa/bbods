# bbods — сводный статус и план (на момент 24.09.2026)

Репозиторий: `slonexa/bbods`. Этот файл — единая точка сборки всего обсуждённого:
что чинить, что делать прямо сейчас, что отложено, и почему.

---

## 1. Цель проекта

Найти реальные, воспроизводимые схемы вокруг prediction markets (в первую очередь
Bybit Odds vs Polymarket/другие CEX) — либо структурный арбитраж, либо статистический эдж —
и по возможности использовать активность на Bybit Odds для участия в текущем промо
(лидерборд по объёму), не теряя деньги бездумно.

---

## 2. Баги, которые нужно поправить (см. также `bbods-fixes-instruction.md` в репо)

### Фикс 1 (приоритет выше): `is_arb` не учитывает расхождение времени экспирации
Файлы: `normalizer/mapper.py` (`auto_match_crypto_targets`), `engine/spread.py` (`process_matches`).
`time_warning` уже считается, но не блокирует `is_arb`. Добавить порог
`MAX_TIME_DIFF_HOURS = 0.5`, `is_cross_arb = hedge_margin > 0 and abs(time_diff_h) <= MAX_TIME_DIFF_HOURS`,
завести отдельный флаг `is_arb_time_risky` для UI/БД.

### Фикс 2: синтетическая цена противоположной стороны Polymarket
Файл: `engine/spread.py`, `cost_poly_opp = 1.0 - prob_poly` — заменить на реальную запись
противоположного outcome из `poly_data` (она уже собирается в `collectors/polymarket.py`,
просто не используется). Fallback на `1 - prob_poly` оставить только если реальной записи нет,
и в этом случае помечать `opp_price_is_synthetic: true`.

**Важно:** для чисто внутри-байбитовского Dutch Book (UP/DOWN на одном контракте) эти два
фикса не блокируют работу — `calculate_dutch_book` берёт котировки с обеих ног напрямую от
Bybit, Polymarket там не участвует. Значит на задачи из раздела 4 ниже можно не ждать фиксов.

---

## 3. Актуальное промо Bybit Odds (официальные условия, зафиксировано с оригинала)

- **Продукт:** Bybit Odds — контракты с фиксированной доходностью на BTC/ETH, без риска
  ликвидации, максимальный убыток = вся выделенная сумма. Три типа: Up/Down (5 или 15 мин),
  Target (до 7 дней), Range (до 7 дней). Минимальная ставка — 5 USDT.
- **Период промо:** 00:00 UTC 21 сентября – 23:59 UTC 20 октября 2026.
- **Награда 1 — первая сделка нового юзера:** airdrop 5 USDT, первый ордер Odds номиналом
  ≥ 20 USDT за период акции, только для первых 1000 участников по времени (порядок очереди),
  только для тех, у кого не было сделок Odds до начала акции. Зачисление в Бонус-центр,
  действует 30 дней.
- **Награда 2 — лидерборд по объёму:** призовой пул 12,000 USDT, вход от 500 USDT
  накопленного объёма в действительных сделках Odds за период. Места 1–30 получают награды
  (1-е — 5000 USDT, 2-е — 3000, 3-е — 1000, 4–5-е — по 500, 6–10-е — по 200, 11–30-е — по 50).
  Обе награды можно получить одновременно.
- **Снимок таблицы на 3-й день акции (для ориентира по темпу):** #1 ≈ $3,115 объёма,
  #10 ≈ $325. Порог входа низкий, конкуренция пока слабая — окно есть, но будет расти
  весь месяц.
- **Ранжирование — чисто по объёму, без фильтра по PnL.** В тексте самой акции нет явного
  запрета на хедж-пары (UP+DOWN одновременно), но общие правила площадки Bybit отдельно
  почти всегда запрещают мультиаккаунтинг под одного трейдера — не проверено полностью
  (полный Terms & Conditions не читали), решили не рисковать вторым акком.

---

## 4. Решение по хедж-фармингу объёма — ОТЛОЖЕНО, не приоритет сейчас

Механика (для справки, уже реализована в `calculate_dutch_book`): открыть UP и DOWN
одновременно на одном и том же 5/15-мин контракте, стейки по Kelly-split
(`stake_pos_pct`/`stake_neg_pct`) — гарантированно одинаковый результат независимо от исхода,
стоимость = overround площадки (в примере из CHANGELOG ~11% на пару 1.8x/1.8x). Это не
арбитраж, а предсказуемая "покупка объёма" по известной цене.

**Решение на сейчас:** не тратить бюджет на это заранее — неясно, окупится ли объём призом,
и не хочется терять деньги на гарантированный минус без ясности по итогу. Вместо этого:

- Держать эту тактику как **резервный план на последние дни акции** (после 15-17 октября),
  если к тому моменту будет ясно, что для попадания в топ нужен конкретный, посчитанный объём —
  тогда можно осознанно добить нужную сумму зная точную цену вопроса.
- **Не открывать второй аккаунт** под это — риск бана/дисквалификации важнее потенциальной
  выгоды в объёме.
- Перед тем как использовать тактику даже в последние дни — открыть полный Terms & Conditions
  Bybit (не только текст этой акции) и явно проверить формулировки про "artificial/wash trading"
  и "hedged/self-covering positions".

---

## 5. Приоритет №1 прямо сейчас: бессрочный сбор данных без риска денег

Цель — проверить на реальных логах гипотезы 4.4 и 4.5 из `arbitrage-scraper-spec.md`
(лаг обновления коэффициента и моментум-продолжение движения), не тратя ни доллара.

**Конкретное ТЗ для Antigravity:**

1. Отдельный лёгкий процесс (можно в том же `run.py` или отдельным скриптом), который для
   всех активных 5-мин и 15-мин Up/Down контрактов BTC/ETH на Bybit пишет тик **раз в секунду**
   (не раз в 10 сек, как основной цикл) в новую таблицу `sec_ticks`:
   ```sql
   CREATE TABLE sec_ticks (
     id INTEGER PRIMARY KEY,
     symbol TEXT,
     odds_up REAL, odds_down REAL,
     spot_price REAL,
     seconds_to_expiry REAL,
     timestamp TEXT
   )
   ```
2. Логировать это **весь жизненный цикл** каждого окна (не только последние 30-60 сек) — это
   нужно и под 4.4 (лаг у экспирации), и под 4.5 (моментум в первые минуты окна).
3. `spot_price` — брать из того же тикера, что уже приходит по WS от Bybit (или отдельный
   паблик тикер как эталон, если Bybit не отдаёт чистую спот-цену отдельно от своей odds-цены).
4. Никаких реальных ордеров на этом этапе. Просто дать логу копиться минимум 3-5 дней,
   желательно захватить и спокойные, и резко-волатильные периоды.
5. Когда данных наберётся — прогнать анализ отдельно по двум вопросам:
   - **4.4 (лаг):** как быстро `odds_up`/`odds_down` реагируют на изменение `spot_price` в
     последние 60 секунд перед экспирацией — есть ли систематическая задержка.
   - **4.5 (моментум):** после движения `spot_price` на X% в первые 1-2 минуты окна, как часто
     направление сохранялось до экспирации против того, как часто разворачивалось.
   Оба вопроса проверять на out-of-sample отрезке (не решать на той же выборке, где искали
   закономерность), чтобы не словить переобучение на шуме 5-минуток.

---

## 7. Новое наблюдение (24.09): у Polymarket тоже есть 5/15-мин Up/Down

Подтверждено скриншотами реального аккаунта — Polymarket предлагает точно такие же
короткие Up/Down окна (5 мин, 15 мин) по BTC/ETH, как Bybit/MEXC. Это меняет раздел 8.1 —
прямая пара для хеджа/сравнения с Bybit 5/15-мин теперь реально существует, не нужно
ограничиваться Target-по-дате как единственным совместимым форматом.

## 8. Ручной лог попыток (вести прямо сейчас, до автоматизации)

По ходу ручной торговли уже была замечена связка Bybit 5/15-мин + Polymarket/MEXC —
в одной попытке сработал аномально выгодный коэффициент и сделка вышла в плюс, но также
были случаи "не успел" и "коэф обманул" (запрошенная цена разошлась с исполненной). Чтобы
отличить реальный паттерн от везения/шума, нужно логировать **каждую** попытку, не только
удачные. Таблица (можно как CSV в репо, `manual_trades_log.csv`):

| Поле | Что писать |
|---|---|
| timestamp | время попытки открытия, до секунды |
| platform_a / platform_b | какие площадки/окна использовались (напр. bybit_5min, polymarket_15min) |
| requested_odds | коэффициент/цена, которую видел на экране в момент решения |
| filled_odds | коэффициент/цена, по которой реально исполнилось (если исполнилось) |
| outcome | win / loss / missed_timing / odds_moved_before_fill |
| pnl | реальный результат в USDT |
| note | свободный комментарий (например "открыл на 3-й секунде окна Polymarket") |

Отдельно замерить точно (не на глаз): задержку между появлением нового окна на Polymarket
UI и моментом, когда ордер реально принимается (наблюдение было "от 47 до 51 секунды" —
нужно подтвердить на 10+ окнах, стабильно ли это значение или гуляет).

Через 20-30 записей в логе — отдельный сеанс разбора: сколько раз "дикий коэффициент"
реально повторился, и коррелирует ли он с чем-то измеримым (время до экспирации, размер
недавнего движения цены), а не был разовым везением.

---

## 10. Апдейт 25.09: фиксы подтверждены, найден модуль лага, есть раздвоение стека

- **Фикс 1 и Фикс 2 (раздел 2) — подтверждены в коде и покрыты тестами.** `tests/test_spread_engine.py`
  проверяет оба случая (блокировка `is_arb` при расхождении времени >0.5ч, приоритет реальной
  цены Polymarket над синтетической). Прогнано локально: `2 passed`.
- **Найден готовый модуль замера лага** — `test/prediction-market-quote-comparison (2)/src/lib/engine/lag.ts`.
  Реализует ровно методологию из раздела 5: находит момент движения спота, ищет, через сколько
  времени изменился коэффициент, копит `p90`/медиану лага, даёт вывод (лаг <0.7s → паттерна нет,
  1-3s → погранично, 3s+ → есть edge). Корректно учитывает, что лаг, равный интервалу опроса,
  значит "не измерено", а не "есть edge" — не переносить бездумно, но методика верная.
- **⚠️ Открытый вопрос: раздвоение стека.** В `test/` появились два почти идентичных Next.js/TS
  проекта (`prediction-market-quote-comparison` и `...(2)`), у которых есть модуль лага, но
  **нет** ни Фикса 1, ни Фикса 2 — если реально смотреть дашборд этой версии, а не Python,
  можно увидеть непроверенные "вилки". Нужно решить: TS — это будущий основной стек (тогда
  перенести оба фикса туда, удалить одну из двух копий) или разовая проба ради `lag.ts`
  (тогда перенести саму идею лага в Python-версию и почистить `test/`).

---

## 12. Апдейт 25.09 (второй): большой коммит — 1Hz логгер, авто-paper-trading, аналитика, Telegram

Новые файлы: `engine/sec_logger.py`, `engine/auto_paper.py`, `engine/alerts.py`,
`scripts/analyze_hypotheses.py`, P&L-терминал в дашборде. Проверено:

- **Безопасность подтверждена:** `auto_paper.py` — чистая симуляция, никаких API-ключей/реальных
  ордеров, только виртуальная таблица `paper_trades`. `.gitignore` правильно исключает
  `alert_config.json` — Telegram-токен не утечёт в публичный репозиторий.
- **Сами нашли и починили важный баг** ("Frozen RAM Cache" — Bybit WS не обновляет Target-опционы,
  сканер показывал устаревшие/сдвинутые котировки как "вилку"). Хороший знак самостоятельного
  контроля качества данных.
- **`auto_paper.py` тестирует все 4 гипотезы отдельными изолированными портфелями**
  (`sync_start_arb`, `two_step_hedge`, `poly_48s_lag`, `updown_5m`) — не смешивая результаты.
- **`sec_logger.py` тянет реальный ордербук Polymarket** (`clob.polymarket.com/books`) и считает
  `cross_hedge_cost` по настоящим ask-ценам — тот же принцип, что и Фикс 2, применён и в новом коде.

**⚠️ Главное, что нужно поправить:** `scripts/analyze_hypotheses.py` объявляет "🟢 ЭДЖ ОБНАРУЖЕН"
при Win Rate > 55.55% **без проверки статистической значимости** (нет доверительного интервала/
биномиального теста, нет минимального порога выборки). На 30-50 окнах случайный шум легко даёт
60%+ винрейт без реального эджа — риск преждевременно поверить в паттерн и перейти на реальные
деньги. Добавить: биномиальный тест против 55.55%, не показывать вывод "эдж найден" при выборке
< 100-150 повторений.

**Второстепенное:** методика лага в `analyze_hypotheses.py` (раздел 4.4) грубее, чем уже готовый
`lag.ts` в TS-версии (там честный мс-лаг между конкретным движением и конкретным откликом, тут —
два независимых счётчика без прямой привязки). Перенести методику из `lag.ts`, раз Python —
основной стек. Вопрос по двум дублям Next.js-проекта в `test/` — всё ещё открыт.

---

## 13. Общий порядок действий (актуальный, на 25.09, третий апдейт)

1. ~~Влить Фикс 1 и Фикс 2~~ — готово, подтверждено тестами.
2. ~~**Добавить статистическую проверку в `analyze_hypotheses.py`** (раздел 12)~~ — **готово**: внедрены односторонний точный биномиальный тест (`binomial_p_value` против 55.56%), 95% доверительный интервал Уилсона (`wilson_ci_95`), хронологический сплит **In-Sample (50%) / Out-of-Sample (50%)** и строгий гейт выборки `MIN_SAMPLE_SIZE = 100` (вывод об эдже блокируется при `N < 100` или `p-value >= 0.05`).
3. ~~**Перенести методику `lag.ts` в Python и решить судьбу TS-стека в `test/`** (раздел 10)~~ — **готово**: алгоритм точного мс-лага между конкретным импульсом спота (`>= 0.02%`) и первым последующим откликом котировки перенесен в `scripts/analyze_hypotheses.py` (`compute_lag_study`) и выведен в UI полигона (`/api/hypotheses_stats`). Оба черновых Next.js-проекта из `test/` перенесены в архив `backups/legacy_ts_prototype/`, основной и единственный рабочий стек — Python. Покрыто юнит-тестами (`tests/test_spread_engine.py`: `4 passed`).
4. **Дать `sec_logger.py` + `auto_paper.py` покрутиться ещё несколько дней, копить выборку** — сейчас в `ticks.db` уже **85,600+ тиков** и **142 полных 5-минутных окна** (из них `N = 62` с импульсом `>= 0.03%`; текущий WR = `74.19%`, `p = 0.0019`, но гейт `N < 100` держит статус `⏳ МАЛО ДАННЫХ`, пока не наберется 100+ импульсных окон).
5. Первая сделка на 20+ USDT для награды 1 — если ещё не сделана, до заполнения очереди.
6. Когда выборка перевалит за `N >= 100–150` на каждую гипотезу — сеанс разбора по всем 4 гипотезам (`sync_start_arb`, `two_step_hedge`, `poly_48s_lag`, `updown_5m`) плюс ручной лог из раздела 8.
7. Если нашёлся статистически значимый паттерн (`N >= 100`, `p < 0.05` на Out-of-Sample) — проверить руками на минимальной сумме, только потом думать об автоматизации именно под этот паттерн.
8. Хедж-фарминг объёма (раздел 4) — резерв на последние дни акции (после 15-17 октября).

